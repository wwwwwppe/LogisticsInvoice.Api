# 学习任务

## 使用方式

一次只完成一个任务。完成后不要让 AI 先修改代码，直接回复：

> 学习任务 1 已完成，请评分，先不要修改代码。

评分时会先运行构建和接口验收，再给出分数、证据和改进建议。你可以先做第二次修改，最后才看参考实现。

## 学习任务 1｜按 ID 查询客户/供应商

### 目标接口

```text
GET /api/business-parties/{id}
```

### 需要完成的位置

1. `BusinessPartyService.GetByIdAsync`
2. `BusinessPartiesController.GetById`

请搜索：

```text
TODO(学习任务 1)
```

### 功能要求

- ID 存在时返回 HTTP 200。
- 响应体必须使用 `ApiResponse<BusinessPartyDto>`。
- ID 不存在时返回 HTTP 404。
- 404 响应仍然使用项目统一错误结构。
- Controller 不得直接访问 Repository 或 `AppDbContext`。
- 不要复制已经存在的“查询不到则抛出 404”逻辑。
- 不要为了完成本题引入 AutoMapper、通用 Repository 或其他新框架。

### 自测步骤

1. 通过 Swagger 新增一条客户数据，记住返回的 ID。
2. 调用 `GET /api/business-parties/{id}`，检查字段和 HTTP 200。
3. 查询一个不存在的 ID，例如 `999999`，检查 HTTP 404 和统一错误结构。
4. 执行：

```powershell
dotnet build LogisticsInvoice.sln --no-restore
```

### 提交前请准备口头回答

1. 为什么查询不到的判断放在 Service，而不是 Controller？
2. 为什么接口返回 DTO，而不是 `BusinessParty` Entity？
3. 为什么应该复用现有私有方法？
4. `ActionResult<ApiResponse<BusinessPartyDto>>` 各层泛型分别代表什么？

### 评分标准

| 项目 | 分数 |
|---|---:|
| 功能正确，200 与 404 均符合要求 | 40 |
| Controller、Service、Repository 职责清楚 | 20 |
| 复用已有逻辑，没有不必要重复 | 15 |
| 统一响应和 HTTP 状态码正确 | 10 |
| 命名、异步写法和可读性 | 10 |
| 能回答上述设计问题 | 5 |

总分 100 分。

### 完成记录

- 2026-07-01：构建成功，Oracle 实测存在 ID 返回 200、不存在 ID 返回统一 404，学习任务 1 通过。
- 当前实现已正确使用 `async/await`、复用 `GetRequiredAsync` 并通过 `ToDto` 返回 DTO。
- 后续提交前仍应清理已完成的 TODO、旧实现注释和不规范空格。

## 学习任务 2｜为 Service 的 404 分支编写单元测试

### 学习目标

- 理解单元测试为什么不需要启动 Web API 或 Oracle。
- 学会使用 xUnit、Moq 和 Arrange-Act-Assert 结构。
- 学会验证异步异常、HTTP 状态信息和依赖调用次数。

### 需要完成的位置

```text
tests/LogisticsInvoice.Api.Tests/Services/BusinessPartyServiceTests.cs
```

请搜索：

```text
TODO(学习任务 2)
```

只完成 `GetByIdAsync_WhenMissing_Throws404BusinessException`，不要修改生产代码。文件中已经提供“查询成功”的测试作为参考。

### 功能要求

- Mock `IBusinessPartyRepository.GetByIdAsync`，让指定 ID 返回 `null`。
- 调用 `BusinessPartyService.GetByIdAsync`。
- 使用异步异常断言捕获 `BusinessException`。
- 断言 `StatusCode` 为 HTTP 404。
- 断言异常消息包含查询的 ID。
- 验证 Repository 的查询方法只调用一次。
- 测试中不得连接 Oracle、启动 API 或访问真实 HTTP 接口。

### 自测步骤

```powershell
dotnet test LogisticsInvoice.sln --no-restore
```

完成前，测试任务应有一个通过、一个失败；完成后应当两个都通过。

### 提交前请准备口头回答

1. 为什么这个测试不需要连接 Oracle？
2. Mock Repository 的作用是什么？
3. 为什么异步异常要使用 `Assert.ThrowsAsync<T>`？
4. 单元测试和接口集成测试分别负责验证什么？

### 评分标准

| 项目 | 分数 |
|---|---:|
| 正确 Mock Repository 返回 null | 20 |
| 正确捕获 `BusinessException` | 25 |
| 断言 404 和包含 ID 的错误消息 | 20 |
| 验证 Repository 只调用一次 | 10 |
| Arrange-Act-Assert、异步写法和可读性 | 15 |
| 能回答上述设计问题 | 10 |

总分 100 分。
